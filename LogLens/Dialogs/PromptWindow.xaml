<Window x:Class="LogLens.Dialogs.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LogLens" SizeToContent="Height" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource App.Bg}">
    <StackPanel Margin="16">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,10"
                   Style="{StaticResource Dim}" />
        <TextBox x:Name="Input" Padding="6,5" FontFamily="Consolas"
                 KeyDown="Input_KeyDown" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="88" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="OK" Width="88" IsDefault="True" Style="{StaticResource AccentButton}"
                    Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
